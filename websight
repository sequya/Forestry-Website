<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Forestry Interactive Website</title>
<style>
  :root {
    --dark-green: #1b3a2d;
    --medium-green: #2f6f4e;
    --light-green: #a3d9a5;
    --cream: #f9f9f4;
    --orange: #e67e22;
  }
  * {
    box-sizing: border-box;
  }
  body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    margin: 0; background: var(--cream); color: var(--dark-green);
    display: flex; flex-direction: column; min-height: 100vh;
  }
  header {
    background: var(--dark-green);
    color: var(--cream);
    padding: 1.5rem 1rem;
    text-align: center;
    font-size: 2rem;
    font-weight: 700;
    letter-spacing: 1.5px;
  }
  nav {
    background: var(--medium-green);
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    padding: 1rem 0;
    flex-wrap: wrap;
  }
  nav button {
    background: none;
    border: none;
    color: var(--cream);
    font-weight: 600;
    font-size: 1.1rem;
    cursor: pointer;
    padding: 0.5rem 1rem;
    border-radius: 5px;
    transition: background-color 0.3s ease;
  }
  nav button:hover, nav button.active {
    background: var(--orange);
    color: var(--dark-green);
  }
  main {
    flex: 1;
    max-width: 900px;
    margin: 2rem auto;
    padding: 0 1rem;
  }
  section {
    display: none;
    animation: fadeIn 0.6s ease forwards;
  }
  section.active {
    display: block;
  }
  h2 {
    color: var(--medium-green);
    margin-bottom: 1rem;
  }
  p, li {
    font-size: 1.1rem;
    line-height: 1.6;
  }
  ul {
    list-style-type: square;
    margin-left: 1.5rem;
  }
  footer {
    background: var(--dark-green);
    color: var(--cream);
    text-align: center;
    padding: 1rem 0;
    font-size: 0.9rem;
    margin-top: auto;
  }
  /* Interactive collapsible */
  .collapsible {
    background-color: var(--light-green);
    color: var(--dark-green);
    cursor: pointer;
    padding: 0.75rem 1rem;
    width: 100%;
    border: none;
    text-align: left;
    outline: none;
    font-size: 1.1rem;
    font-weight: 600;
    border-radius: 5px;
    margin-top: 1rem;
  }
  .activeCollapsible, .collapsible:hover {
    background-color: var(--medium-green);
    color: var(--cream);
  }
  .contentCollapsible {
    padding: 0 1rem;
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease;
    background-color: #d9ead3;
    border-radius: 0 0 5px 5px;
  }
  /* Contact form */
  form {
    max-width: 500px;
    margin-top: 1rem;
  }
  label {
    display: block;
    margin-top: 1rem;
    font-weight: 600;
  }
  input[type="text"], input[type="email"], textarea {
    width: 100%;
    padding: 0.6rem;
    margin-top: 0.3rem;
    border: 2px solid var(--medium-green);
    border-radius: 4px;
    font-size: 1rem;
    resize: vertical;
  }
  button.submit-btn {
    margin-top: 1.5rem;
    background-color: var(--orange);
    color: var(--dark-green);
    border: none;
    padding: 0.7rem 1.5rem;
    font-weight: 700;
    cursor: pointer;
    border-radius: 5px;
    transition: background-color 0.3s ease;
  }
  button.submit-btn:hover {
    background-color: #c35d11;
    color: #fff;
  }
  /* Fade in animation */
  @keyframes fadeIn {
    from {opacity: 0;}
    to {opacity: 1;}
  }
  /* Responsive */
  @media (max-width: 600px) {
    nav {
      flex-direction: column;
      gap: 0.5rem;
    }
  }
</style>
</head>
<body>
<header>
  Forestry Interactive Website
</header>
<nav>
  <button class="nav-btn active" data-target="home">Home</button>
  <button class="nav-btn" data-target="about">About Forestry</button>
  <button class="nav-btn" data-target="types">Types of Forests</button>
  <button class="nav-btn" data-target="conservation">Conservation</button>
  <button class="nav-btn" data-target="contact">Contact</button>
</nav>

<main>
  <section id="home" class="active">
    <h2>Welcome to Forestry World</h2>
    <p>Forests cover about 31% of the planet’s land area and provide vital resources, habitat for wildlife, and a key role in climate regulation.</p>
    <p>Explore our site to learn more about the types of forests, the importance of forestry, and ways to help conserve these precious ecosystems.</p>
    <button class="collapsible">Why are forests important?</button>
    <div class="contentCollapsible">
      <p>Forests act as the Earth's lungs by absorbing carbon dioxide and producing oxygen. They provide habitat for over 80% of terrestrial species, prevent soil erosion, and support livelihoods worldwide.</p>
    </div>
  </section>

  <section id="about">
    <h2>About Forestry</h2>
    <p>Forestry is the science and craft of creating, managing, using, conserving, and repairing forests to meet desired goals and needs for human and environmental benefits.</p>
    <button class="collapsible">What does a forester do?</button>
    <div class="contentCollapsible">
      <p>Foresters plan and implement forest management strategies, monitor forest health, oversee harvesting, and work on conservation and reforestation projects.</p>
    </div>
  </section>

  <section id="types">
    <h2>Types of Forests</h2>
    <ul>
      <li><strong>Tropical Forests:</strong> Located near the equator, hot and humid year-round, very biodiverse.</li>
      <li><strong>Temperate Forests:</strong> Experience four distinct seasons, rich in hardwood trees.</li>
      <li><strong>Boreal Forests (Taiga):</strong> Found in northern regions, dominated by coniferous trees, cold climate.</li>
    </ul>
    <button class="collapsible">More about Tropical Forests</button>
    <div class="contentCollapsible">
      <p>Tropical forests are home to more species of plants and animals than any other terrestrial ecosystem and are vital for global oxygen and carbon cycles.</p>
    </div>
  </section>

  <section id="conservation">
    <h2>Forest Conservation</h2>
    <p>Conservation involves protecting forest ecosystems from deforestation, degradation, and fragmentation while promoting sustainable use.</p>
    <ul>
      <li>Reforestation and afforestation</li>
      <li>Protected areas and national parks</li>
      <li>Sustainable logging practices</li>
      <li>Community involvement and education</li>
    </ul>
    <button class="collapsible">How can you help?</button>
    <div class="contentCollapsible">
      <p>You can support forest conservation by planting trees, reducing paper waste, supporting sustainable products, and raising awareness about forest protection.</p>
    </div>
  </section>

  <section id="contact">
    <h2>Contact Us</h2>
    <form id="contactForm" novalidate>
      <label for="name">Name:</label>
      <input type="text" id="name" name="name" required placeholder="Your full name" />
      
      <label for="email">Email:</label>
      <input type="email" id="email" name="email" required placeholder="example@domain.com" />
      
      <label for="message">Message:</label>
      <textarea id="message" name="message" rows="5" required placeholder="Write your message here"></textarea>
      
      <button type="submit" class="submit-btn">Send Message</button>
    </form>
    <p id="formMessage" style="margin-top:1rem;"></p>
  </section>
</main>

<footer>
  &copy; 2025 Forestry Interactive Website
</footer>

<script>
  // Navigation functionality
  const navButtons = document.querySelectorAll('nav button.nav-btn');
  const sections = document.querySelectorAll('main section');

  navButtons.forEach(button => {
    button.addEventListener('click', () => {
      // Remove active class on all buttons and sections
      navButtons.forEach(btn => btn.classList.remove('active'));
      sections.forEach(sec => sec.classList.remove('active'));

      // Add active class to clicked button and corresponding section
      button.classList.add('active');
      const target = button.getAttribute('data-target');
      document.getElementById(target).classList.add('active');
      window.scrollTo({top: 0, behavior: 'smooth'});
    });
  });

  // Collapsible sections
  const collapsibles = document.querySelectorAll('.collapsible');
  collapsibles.forEach(btn => {
    btn.addEventListener('click', () => {
      btn.classList.toggle('activeCollapsible');
      const content = btn.nextElementSibling;
      if (content.style.maxHeight) {
        content.style.maxHeight = null;
      } else {
        content.style.maxHeight = content.scrollHeight + "px";
      }
    });
  });

  // Contact form validation and submission (simulated)
  const form = document.getElementById('contactForm');
  const formMessage = document.getElementById('formMessage');

  form.addEventListener('submit', (e) => {
    e.preventDefault();

    // Simple validation
    const name = form.name.value.trim();
    const email = form.email.value.trim();
    const message = form.message.value.trim();

    if (!name || !email || !message) {
      formMessage.style.color = 'red';
      formMessage.textContent = 'Please fill in all fields.';
      return;
    }
    if (!validateEmail(email)) {
      formMessage.style.color = 'red';
      formMessage.textContent = 'Please enter a valid email address.';
      return;
    }

    // Simulate form submission success
    formMessage.style.color = 'green';
    formMessage.textContent = 'Thank you for contacting us! We will get back to you soon.';
    form.reset();
  });

  function validateEmail(email) {
    // Basic email regex
    const re = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    return re.test(email.toLowerCase());
  }
</script>
</body>
</html>
